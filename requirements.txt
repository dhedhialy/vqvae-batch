# Core
torch>=1.13.0
numpy>=1.23.0
scikit-learn>=1.1.0
# Real data loading & preprocessing
anndata>=0.10.0
scanpy>=1.10.0

# Evaluation metrics (batch mixing / biological conservation)
pandas>=1.5.0

# scVI/scANVI baselines only (push_scvi.py, atlas_eval/baselines.py) — heavy,
# install separately if reproducing baseline rows: scvi-tools>=1.1.0
